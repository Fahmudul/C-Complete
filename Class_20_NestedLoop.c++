#include <iostream>
#include <ctime>
#include <cstdlib>
int main()
{
  // Nested loop
  int i;
  // srand(time(NULL));
  // for (i = 0; i < 7; i++)
  // {
  //   int num = (rand() % 6) + 1;
  //   std::cout << num << std::endl;
  // }
  // int num;
  // for (i = 0; i < 7; i++)
  // {
  //   for (int j = 0; j < 7; j++)
  //   {
  //     num = (rand() % 6) + 1;
  //     std::cout << num << ' ';
  //   }
  //   std::cout << '\n';
  // }

  

  return 0;
}